# George Build Logic

This included build hosts project-specific Gradle convention plugins.
It follows the same high-level structure used by Now in Android, adapted for
the `george.*` plugin namespace and this template project's current scope.
